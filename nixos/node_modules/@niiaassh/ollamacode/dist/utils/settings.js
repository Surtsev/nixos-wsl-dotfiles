"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadSettings = loadSettings;
exports.saveSettings = saveSettings;
exports.updateSetting = updateSetting;
exports.getSetting = getSetting;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const SETTINGS_DIR = ".ollamacode";
const SETTINGS_FILE = "settings.json";
function getSettingsPath() {
    return path_1.default.join(process.cwd(), SETTINGS_DIR, SETTINGS_FILE);
}
function ensureSettingsDirectory() {
    const settingsDir = path_1.default.join(process.cwd(), SETTINGS_DIR);
    if (!fs_1.default.existsSync(settingsDir)) {
        fs_1.default.mkdirSync(settingsDir, { recursive: true });
    }
}
const DEFAULT_SETTINGS = {
    model: "qwen3:8b",
};
function loadSettings() {
    try {
        ensureSettingsDirectory();
        const settingsPath = getSettingsPath();
        if (!fs_1.default.existsSync(settingsPath)) {
            saveSettings(DEFAULT_SETTINGS);
            return DEFAULT_SETTINGS;
        }
        const settingsContent = fs_1.default.readFileSync(settingsPath, "utf-8");
        return JSON.parse(settingsContent);
    }
    catch (error) {
        console.warn("Failed to load settings:", error);
        return {};
    }
}
function saveSettings(settings) {
    try {
        ensureSettingsDirectory();
        const settingsPath = getSettingsPath();
        fs_1.default.writeFileSync(settingsPath, JSON.stringify(settings, null, 2));
    }
    catch (error) {
        console.error("Failed to save settings:", error);
    }
}
function updateSetting(key, value) {
    const currentSettings = loadSettings();
    currentSettings[key] = value;
    saveSettings(currentSettings);
}
function getSetting(key) {
    const settings = loadSettings();
    return settings[key];
}
//# sourceMappingURL=settings.js.map