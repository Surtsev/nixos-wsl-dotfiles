"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ollamacodeAgent = void 0;
const client_1 = require("../ollamacode/client");
const tools_1 = require("../ollamacode/tools");
const index_1 = require("../tools/index");
const events_1 = require("events");
const token_counter_1 = require("../utils/token-counter");
const custom_instructions_1 = require("../utils/custom-instructions");
class ollamacodeAgent extends events_1.EventEmitter {
    constructor(model) {
        super();
        this.chatHistory = [];
        this.messages = [];
        this.abortController = null;
        const modelToUse = "qwen3:8b"; // Default model
        this.ollamacodeClient = new client_1.ollamacodeClient(model);
        this.textEditor = new index_1.TextEditorTool();
        this.bash = new index_1.BashTool();
        this.todoTool = new index_1.TodoTool();
        this.confirmationTool = new index_1.ConfirmationTool();
        this.search = new index_1.SearchTool();
        this.tokenCounter = (0, token_counter_1.createTokenCounter)(modelToUse);
        // Initialize MCP servers if configured
        ;
        // Load custom instructions
        const customInstructions = (0, custom_instructions_1.loadCustomInstructions)();
        const customInstructionsSection = customInstructions
            ? `\n\nCUSTOM INSTRUCTIONS:\n${customInstructions}\n\nThe above custom instructions should be followed alongside the standard instructions below.`
            : "";
        // Initialize with system message
        this.messages.push({
            role: "system",
            content: `You are ollamacode CLI, an AI assistant that helps with file editing, coding tasks, and system operations.${customInstructionsSection}

You have access to these tools:
- view_file: View file contents or directory listings
- create_file: Create new files with content (ONLY use this for files that don't exist yet)
- str_replace_editor: Replace text in existing files (ALWAYS use this to edit or update existing files)
- bash: Execute bash commands (use for searching, file discovery, navigation, and system operations)
- search: Unified search tool for finding text content or files (similar to Cursor's search functionality)
- create_todo_list: Create a visual todo list for planning and tracking tasks
- update_todo_list: Update existing todos in your todo list

REAL-TIME INFORMATION:
You have access to real-time web search with bash tool. When users ask for current information,latest news, or recent events, you automatically have access to up-to-date information from the web and social media.

IMPORTANT TOOL USAGE RULES:
- NEVER use create_file on files that already exist - this will overwrite them completely
- ALWAYS use str_replace_editor to modify existing files, even for small changes
- Before editing a file, use view_file to see its current contents
- Use create_file ONLY when creating entirely new files that don't exist

SEARCHING AND EXPLORATION:
- Use search for fast, powerful text search across files or finding files by name (unified search tool)
- Examples: search for text content like "import.*react", search for files like "component.tsx"
- Use bash with commands like 'find', 'grep', 'rg', 'ls' for complex file operations and navigation
- view_file is best for reading specific files you already know exist

When a user asks you to edit, update, modify, or change an existing file:
1. First use view_file to see the current contents
2. Then use str_replace_editor to make the specific changes
3. Never use create_file for existing files

When a user asks you to create a new file that doesn't exist:
1. Use create_file with the full content

TASK PLANNING WITH TODO LISTS:
- For complex requests with multiple steps, ALWAYS create a todo list first to plan your approach
- Use create_todo_list to break down tasks into manageable items with priorities
- Mark tasks as 'in_progress' when you start working on them (only one at a time)
- Mark tasks as 'completed' immediately when finished
- Use update_todo_list to track your progress throughout the task
- Todo lists provide visual feedback with colors: ✅ Green (completed), 🔄 Cyan (in progress), ⏳ Yellow (pending)
- Always create todos with priorities: 'high' (🔴), 'medium' (🟡), 'low' (🟢)

USER CONFIRMATION SYSTEM:
File operations (create_file, str_replace_editor) and bash commands will automatically request user confirmation before execution. The confirmation system will show users the actual content or command before they decide. Users can choose to approve individual operations or approve all operations of that type for the session.

If a user rejects an operation, the tool will return an error and you should not proceed with that specific operation.

Be helpful, direct, and efficient. Always explain what you're doing and show the results.

IMPORTANT RESPONSE GUIDELINES:
- After using tools, do NOT respond with pleasantries like "Thanks for..." or "Great!"
- Only provide necessary explanations or next steps if relevant to the task
- Keep responses concise and focused on the actual work being done
- If a tool execution completes the user's request, you can remain silent or give a brief confirmation

Current working directory: ${process.cwd()}`,
        });
    }
    isollamacodeModel() {
        const currentModel = this.ollamacodeClient.getCurrentModel();
        // console.log("[AGENT] Current model:", currentModel);
        return !!currentModel;
    }
    async processUserMessage(message) {
        // Wait for MCP initialization before processing
        //await this.waitForMCPInitialization();
        // Add user message to conversation
        // console.log("[AGENT] Processing user message:", message);
        const userEntry = {
            type: "user",
            content: message,
            timestamp: new Date(),
        };
        //console.log("[AGENT] User message:", message);
        this.chatHistory.push(userEntry);
        this.messages.push({ role: "user", content: message });
        //console.log("[AGENT] Messages after user entry:", this.messages);
        const newEntries = [userEntry];
        const maxToolRounds = 10; // Prevent infinite loops
        let toolRounds = 0;
        // console.log("[AGENT] Starting chat with ollamacode...");
        try {
            const tools = await tools_1.ollamacode_TOOLS;
            //console.log("[AGENT] Available tools:", tools.map((t) => t));
            let currentResponse = await this.ollamacodeClient.chat(this.messages, tools, { search_parameters: { mode: "auto" } });
            // console.log("[AGENT] Initial response from ollamacode:", currentResponse.choices[0]?.message);
            // Agent loop - continue until no more tool calls or max rounds reached
            while (toolRounds < maxToolRounds) {
                const assistantMessage = currentResponse.choices[0]?.message;
                if (!assistantMessage) {
                    throw new Error("No response from ollamacode");
                }
                // console.log("[AGENT] Assistant message:", assistantMessage);
                // Handle tool calls
                if (assistantMessage.toolCalls &&
                    assistantMessage.toolCalls.length > 0) {
                    toolRounds++;
                    // Add assistant message with tool calls
                    const assistantEntry = {
                        type: "assistant",
                        content: assistantMessage.content || "Using tools to help you...",
                        timestamp: new Date(),
                        toolCalls: assistantMessage.toolCalls || undefined,
                    };
                    //console.log("[AGENT] Assistant entry:", assistantEntry);
                    this.chatHistory.push(assistantEntry);
                    newEntries.push(assistantEntry);
                    // console.log("[AGENT] Tool calls detected:", assistantMessage.tool_calls);
                    // Add assistant message to conversation
                    this.messages.push({
                        role: "assistant",
                        content: assistantMessage.content || "",
                        toolCalls: assistantMessage.toolCalls,
                    });
                    // Create initial tool call entries to show tools are being executed
                    assistantMessage.toolCalls.forEach((toolCall) => {
                        const toolCallEntry = {
                            type: "tool_call",
                            content: "Executing...",
                            timestamp: new Date(),
                            toolCall: toolCall,
                        };
                        //console.log("[AGENT] Tool call entry:", toolCallEntry);
                        this.chatHistory.push(toolCallEntry);
                        newEntries.push(toolCallEntry);
                    });
                    // Execute tool calls and update the entries
                    for (const toolCall of assistantMessage.toolCalls) {
                        // console.log("[AGENT] Executing tool call:", toolCall);
                        const result = await this.executeTool(toolCall);
                        //console.log("agnet resuly", result);
                        // Update the existing tool_call entry with the result
                        const entryIndex = this.chatHistory.findIndex((entry) => entry.type === "tool_call" && entry.toolCall?.id === toolCall.id);
                        // console.log("[AGENT] Executing tool:", toolCall.name);
                        if (entryIndex !== -1) {
                            const updatedEntry = {
                                ...this.chatHistory[entryIndex],
                                type: "tool_result",
                                content: result.success
                                    ? result.output || "Success"
                                    : result.error || "Error occurred",
                                toolResult: result,
                            };
                            this.chatHistory[entryIndex] = updatedEntry;
                            // Also update in newEntries for return value
                            const newEntryIndex = newEntries.findIndex((entry) => entry.type === "tool_call" &&
                                entry.toolCall?.id === toolCall.id);
                            if (newEntryIndex !== -1) {
                                newEntries[newEntryIndex] = updatedEntry;
                            }
                        }
                        // console.log("[AGENT] Tool result:", result);
                        // Add tool result to messages with proper format (needed for AI context)
                        this.messages.push({
                            role: "tool",
                            content: result.success
                                ? result.output || "Success"
                                : result.error || "Error",
                            tool_call_id: toolCall.id,
                        });
                    }
                    // console.log("[AGENT] Tool calls executed:", assistantMessage.toolCalls);
                    // Get next response - this might contain more tool calls
                    currentResponse = await this.ollamacodeClient.chat(this.messages, tools, this.isollamacodeModel()
                        ? { search_parameters: { mode: "auto" } }
                        : undefined);
                }
                else {
                    // No more tool calls, add final response
                    const finalEntry = {
                        type: "assistant",
                        content: assistantMessage.content ||
                            "I understand, but I don't have a specific response.",
                        timestamp: new Date(),
                    };
                    this.chatHistory.push(finalEntry);
                    this.messages.push({
                        role: "assistant",
                        content: assistantMessage.content || "",
                    });
                    newEntries.push(finalEntry);
                    break; // Exit the loop
                }
            }
            if (toolRounds >= maxToolRounds) {
                const warningEntry = {
                    type: "assistant",
                    content: "Maximum tool execution rounds reached. Stopping to prevent infinite loops.",
                    timestamp: new Date(),
                };
                this.chatHistory.push(warningEntry);
                newEntries.push(warningEntry);
            }
            return newEntries;
        }
        catch (error) {
            const errorEntry = {
                type: "assistant",
                content: `Sorry, I encountered an error: ${error.message}`,
                timestamp: new Date(),
            };
            this.chatHistory.push(errorEntry);
            return [userEntry, errorEntry];
        }
    }
    messageReducer(previous, item) {
        //console.log("item", item);
        const reduce = (acc, delta) => {
            acc = { ...acc };
            for (const [key, value] of Object.entries(delta)) {
                if (acc[key] === undefined || acc[key] === null) {
                    acc[key] = value;
                    // Clean up index properties from tool calls
                    if (Array.isArray(acc[key])) {
                        for (const arr of acc[key]) {
                            delete arr.index;
                        }
                    }
                }
                else if (typeof acc[key] === "string" && typeof value === "string") {
                    acc[key] += value;
                }
                else if (Array.isArray(acc[key]) && Array.isArray(value)) {
                    const accArray = acc[key];
                    for (let i = 0; i < value.length; i++) {
                        if (!accArray[i])
                            accArray[i] = {};
                        accArray[i] = reduce(accArray[i], value[i]);
                    }
                }
                else if (typeof acc[key] === "object" && typeof value === "object") {
                    acc[key] = reduce(acc[key], value);
                }
            }
            //console.log("reduced acc", acc);
            return acc;
        };
        // console.log("reducing", previous, item.choices[0]?.delta || {});
        return reduce(previous, item.choices[0]?.delta || {});
    }
    async *processUserMessageStream(message) {
        // Create new abort controller for this request
        this.abortController = new AbortController();
        //console.log("[AGENT] Processing user message:", message);
        // Add user message to conversation
        const userEntry = {
            type: "user",
            content: message,
            timestamp: new Date(),
        };
        this.chatHistory.push(userEntry);
        // console.log("[AGENT] User message:", this.chatHistory);
        this.messages.push({ role: "user", content: message });
        // yield { type: "content", content: `User: ${message}` };
        // console.log("[AGENT] Messages after user entry:", this.messages);
        // Calculate input tokens
        let inputTokens = this.tokenCounter.countMessageTokens(this.messages);
        //console.log("[AGENT] Input tokens:", inputTokens);
        yield {
            type: "token_count",
            tokenCount: inputTokens,
        };
        const maxToolRounds = 30; // Prevent infinite loops
        let toolRounds = 0;
        let totalOutputTokens = 0;
        try {
            // Agent loop - continue until no more tool calls or max rounds reached
            while (toolRounds < maxToolRounds) {
                // Check if operation was cancelled
                if (this.abortController?.signal.aborted) {
                    yield {
                        type: "content",
                        content: "\n\n[Operation cancelled by user]",
                    };
                    yield { type: "done" };
                    return;
                }
                //console.log("[AGENT] Starting chat with ollamacode...");
                // Stream response and accumulate
                const tools = await tools_1.ollamacode_TOOLS;
                const stream = this.ollamacodeClient.chatStream(this.messages, tools, this.isollamacodeModel() ? { search_parameters: { mode: "auto" } } : undefined);
                //console.log("[AGENT] Available tools:", tools.map((t) => t.function.name));
                // console.log("[AGENT] Streaming response from ollamacode...");
                let accumulatedMessage = {};
                let accumulatedContent = "";
                let toolCallsYielded = false;
                for await (const chunk of stream) {
                    //onsole.log("[AGENT] Received chunk:", chunk.choices[0]?.message?.content);
                    // Check for cancellation in the streaming loop
                    if (this.abortController?.signal.aborted) {
                        yield {
                            type: "content",
                            content: "\n\n[Operation cancelled by user]",
                        };
                        yield { type: "done", };
                        return;
                    }
                    // console.log("chunk", chunk.toolCalls || chunk.choices[0]?.message.content);
                    if (!chunk)
                        continue;
                    //onsole.log("[AGENT] Processing chunk:", chunk.choices[0]?.message.content || chunk);
                    // Accumulate the message using reducer
                    accumulatedMessage = this.messageReducer(accumulatedMessage, chunk);
                    // console.log("[AGENT] Accumulated message:",accumulatedMessage);
                    // Check if we have a complete message
                    // Check for tool calls - yield when we have complete tool calls with function names
                    if (!toolCallsYielded && accumulatedMessage.toolCalls?.length > 0) {
                        // Check if we have at least one complete tool call with a function name
                        const hasCompleteTool = accumulatedMessage.toolCalls.some((tc) => tc.name);
                        console.log("hasCompleteTool", hasCompleteTool);
                        if (hasCompleteTool) {
                            yield {
                                type: "tool_calls",
                                toolCalls: accumulatedMessage.toolCalls,
                            };
                            toolCallsYielded = true;
                        }
                    }
                    // console.log("messages", chunk.choices[0].message.content);
                    // Stream content as it comes
                    if (chunk.type === "message") {
                        //console.log("[AGENT] Streaming content:", chunk.choices[0].message.content);
                        console.log("chunk hi ");
                        accumulatedContent += chunk.choices[0].message.content;
                        // console.log("[AGENT] Streaming contentcafter accumulate:", accumulatedContent);
                        // Update token count in real-time including accumulated content and any tool calls
                        const currentOutputTokens = this.tokenCounter.estimateStreamingTokens(accumulatedContent) +
                            (accumulatedMessage.toolCalls
                                ? this.tokenCounter.countTokens(JSON.stringify(accumulatedMessage.toolCalls))
                                : 0);
                        totalOutputTokens = currentOutputTokens;
                        // console.log("[AGENT] Current output tokens:", currentOutputTokens);
                        yield {
                            type: "content",
                            content: chunk.choices[0].message.content,
                        };
                        // Emit token count update
                        yield {
                            type: "token_count",
                            tokenCount: inputTokens + totalOutputTokens,
                        };
                    }
                }
                // Add assistant entry to history
                const assistantEntry = {
                    type: "assistant",
                    content: accumulatedMessage.content || "Using tools to help you...",
                    timestamp: new Date(),
                    toolCalls: accumulatedMessage.toolCalls || undefined,
                };
                this.chatHistory.push(assistantEntry);
                // console.log("[AGENT] Assistant entry:", assistantEntry);
                // Add accumulated message to conversation
                this.messages.push({
                    role: "assistant",
                    content: accumulatedMessage.content || "",
                    toolCalls: accumulatedMessage.toolCalls,
                });
                // Handle tool calls if present
                if (accumulatedMessage.toolCalls?.length > 0) {
                    toolRounds++;
                    // Only yield tool_calls if we haven't already yielded them during streaming
                    if (!toolCallsYielded) {
                        yield {
                            type: "tool_calls",
                            toolCalls: accumulatedMessage.toolCalls,
                        };
                    }
                    // Execute tools
                    for (const toolCall of accumulatedMessage.toolCalls) {
                        // Check for cancellation before executing each tool
                        console.log("[AGENT] Executing tool call:", toolCall);
                        if (this.abortController?.signal.aborted) {
                            yield {
                                type: "content",
                                content: "\n\n[Operation cancelled by user]",
                            };
                            yield { type: "done" };
                            return;
                        }
                        const result = await this.executeTool(toolCall);
                        // console.log("[AGENT] Tool result:", result);
                        const toolResultEntry = {
                            type: "tool_result",
                            content: result.success
                                ? result.output || "Success"
                                : result.error || "Error occurred",
                            timestamp: new Date(),
                            toolCall: toolCall,
                            toolResult: result,
                        };
                        this.chatHistory.push(toolResultEntry);
                        //console.log("[AGENT] Tool result entry:", toolResultEntry);
                        yield {
                            type: "tool_result",
                            toolCall,
                            toolResult: result,
                        };
                        // Add tool result with proper format (needed for AI context)
                        this.messages.push({
                            role: "tool",
                            content: result.success
                                ? result.output || "Success"
                                : result.error || "Error",
                            tool_call_id: toolCall.id,
                        });
                    }
                    // console.log("[AGENT] All tool calls executed:", accumulatedMessage.tool_calls);
                    // Update token count after processing all tool calls to include tool results
                    inputTokens = this.tokenCounter.countMessageTokens(this.messages);
                    yield {
                        type: "token_count",
                        tokenCount: inputTokens + totalOutputTokens,
                    };
                    // Continue the loop to get the next response (which might have more tool calls)
                }
                else {
                    // No tool calls, we're done
                    break;
                }
            }
            if (toolRounds >= maxToolRounds) {
                yield {
                    type: "content",
                    content: "\n\nMaximum tool execution rounds reached. Stopping to prevent infinite loops.",
                };
            }
            yield { type: "done" };
        }
        catch (error) {
            // Check if this was a cancellation
            if (this.abortController?.signal.aborted) {
                yield {
                    type: "content",
                    content: "\n\n[Operation cancelled by user]",
                };
                yield { type: "done" };
                return;
            }
            const errorEntry = {
                type: "assistant",
                content: `Sorry, I encountered an error: ${error.message}`,
                timestamp: new Date(),
            };
            console.log("errir", error.message);
            this.chatHistory.push(errorEntry);
            yield {
                type: "content",
                content: errorEntry.content,
            };
            yield { type: "done" };
        }
        finally {
            // Clean up abort controller
            this.abortController = null;
        }
    }
    async executeTool(toolCall) {
        console.log("[AGENT] Executing:", toolCall);
        try {
            const args = toolCall.args;
            console.log("[AGENT] Executing tool:", toolCall.name, "with args:", args);
            switch (toolCall.name) {
                case "view_file":
                    const range = args.start_line && args.end_line
                        ? [args.start_line, args.end_line]
                        : undefined;
                    return await this.textEditor.view(args.filePath, range);
                case "create_file":
                    return await this.textEditor.create(args.filePath, args.content);
                case "str_replace_editor":
                    return await this.textEditor.strReplace(args.path, args.old_str, args.new_str, args.replace_all);
                case "bash":
                    return await this.bash.execute(args.command);
                case "create_todo_list":
                    return await this.todoTool.createTodoList(args.todos);
                case "update_todo_list":
                    return await this.todoTool.updateTodoList(args.updates);
                case "search":
                    return await this.search.search(args.query, {
                        searchType: args.search_type,
                        includePattern: args.include_pattern,
                        excludePattern: args.exclude_pattern,
                        caseSensitive: args.case_sensitive,
                        wholeWord: args.whole_word,
                        regex: args.regex,
                        maxResults: args.max_results,
                        fileTypes: args.file_types,
                        includeHidden: args.include_hidden,
                    });
                default:
                    // Check if this is an MCP tool
                    return {
                        success: false,
                        error: `Unknown tool: ${toolCall.name}`,
                    };
            }
        }
        catch (error) {
            return {
                success: false,
                error: `Tool execution error: ${error.message}`,
            };
        }
    }
    getChatHistory() {
        return [...this.chatHistory];
    }
    getCurrentDirectory() {
        return this.bash.getCurrentDirectory();
    }
    async executeBashCommand(command) {
        return await this.bash.execute(command);
    }
    getCurrentModel() {
        return this.ollamacodeClient.getCurrentModel();
    }
    setModel(model) {
        this.ollamacodeClient.setModel(model);
        // Update token counter for new model
        this.tokenCounter.dispose();
        this.tokenCounter = (0, token_counter_1.createTokenCounter)(model);
    }
    abortCurrentOperation() {
        if (this.abortController) {
            this.abortController.abort();
        }
    }
}
exports.ollamacodeAgent = ollamacodeAgent;
//# sourceMappingURL=ollamacode-agent.js.map