"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.listModels = listModels;
const child_process_1 = require("child_process");
function runCommand(command) {
    return new Promise((resolve, reject) => {
        (0, child_process_1.exec)(command, (error, stdout, stderr) => {
            if (error) {
                reject(`❌ Error: ${error.message}`);
                return;
            }
            if (stderr) {
                reject(`⚠️ Stderr: ${stderr}`);
                return;
            }
            resolve(stdout.trim());
        });
    });
}
// function specifically for ollama list
async function listModels() {
    try {
        const result = await runCommand("ollama list");
        const lines = result.split("\n").slice(1); // skip header line
        const models = lines.map(line => line.split(/\s+/)[0]); // get model names
        return models;
    }
    catch (err) {
        console.error(err);
    }
}
// testng the listModels function
// async function main() {
//   const models = await listModels();
//   models.forEach(model => console.log(model));
// }
// main();
//# sourceMappingURL=loadmodel.js.map