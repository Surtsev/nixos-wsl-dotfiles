import React from "react";
interface ChatInputProps {
    input: string;
    isProcessing: boolean;
    isStreaming: boolean;
}
export declare function ChatInput({ input, isProcessing, isStreaming, }: ChatInputProps): React.JSX.Element;
export {};
