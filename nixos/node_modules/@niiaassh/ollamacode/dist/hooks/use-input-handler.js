"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useInputHandler = useInputHandler;
const react_1 = require("react");
const ink_1 = require("ink");
const confirmation_service_1 = require("../utils/confirmation-service");
const settings_1 = require("../utils/settings");
const command_suggestions_1 = require("../ui/components/command-suggestions");
const loadmodel_1 = require("../ui/components/loadmodel");
function useInputHandler({ agent, chatHistory, setChatHistory, setIsProcessing, setIsStreaming, setTokenCount, setProcessingTime, processingStartTime, isProcessing, isStreaming, isConfirmationActive = false, }) {
    const [input, setInput] = (0, react_1.useState)("");
    const [showCommandSuggestions, setShowCommandSuggestions] = (0, react_1.useState)(false);
    const [selectedCommandIndex, setSelectedCommandIndex] = (0, react_1.useState)(0);
    const [showModelSelection, setShowModelSelection] = (0, react_1.useState)(false);
    const [selectedModelIndex, setSelectedModelIndex] = (0, react_1.useState)(0);
    const [autoEditEnabled, setAutoEditEnabled] = (0, react_1.useState)(() => {
        const confirmationService = confirmation_service_1.ConfirmationService.getInstance();
        const sessionFlags = confirmationService.getSessionFlags();
        return sessionFlags.allOperations;
    });
    // Removed useApp().exit - using process.exit(0) instead for better terminal handling
    const commandSuggestions = [
        { command: "/help", description: "Show help information" },
        { command: "/clear", description: "Clear chat history" },
        { command: "/models", description: "Switch ollamacode Model" },
        { command: "/commit-and-push", description: "AI commit & push to remote" },
        { command: "/exit", description: "Exit the application" },
    ];
    // Load models asynchronously when component mounts
    async function availableModels() {
        const models = await (0, loadmodel_1.listModels)();
        //console.log("available models in useinputhandler", models);
        // Convert string[] → ModelOption[]
        return models.map(m => ({ model: m }));
    }
    //availableModels().then(model =>{console.log(model.length)}); // just to ensure it's called here
    const handleDirectCommand = async (input) => {
        const trimmedInput = input.trim();
        if (trimmedInput === "/clear") {
            // Reset chat history
            setChatHistory([]);
            // Reset processing states
            setIsProcessing(false);
            setIsStreaming(false);
            setTokenCount(0);
            setProcessingTime(0);
            processingStartTime.current = 0;
            // Reset confirmation service session flags
            const confirmationService = confirmation_service_1.ConfirmationService.getInstance();
            confirmationService.resetSession();
            setInput("");
            return true;
        }
        if (trimmedInput === "/help") {
            const helpEntry = {
                type: "assistant",
                content: `ollamacode CLI Help:

Built-in Commands:
  /clear      - Clear chat history
  /help       - Show this help
  /models     - Ollama models
  /exit       - Exit applicat
  exit, quit  - Exit application
  
Git Commands:
  /commit-and-push - AI-generated commit + push to remote
  
Keyboard Shortcuts:
  Shift+Tab   - Toggle auto-edit mode (bypass confirmations)

Direct Commands (executed immediately):
  ls [path]   - List directory contents
  pwd         - Show current directory  
  cd <path>   - Change directory
  cat <file>  - View file contents
  mkdir <dir> - Create directory
  touch <file>- Create empty file

For complex operations, just describe what you want in natural language.
Examples:
  "edit package.json and add a new script"
  "create a new React component called Header"
  "show me all TypeScript files in this project"`,
                timestamp: new Date(),
            };
            setChatHistory((prev) => [...prev, helpEntry]);
            setInput("");
            return true;
        }
        if (trimmedInput === "/exit") {
            process.exit(0);
            return true;
        }
        if (trimmedInput === "/models") {
            setShowModelSelection(true);
            setSelectedModelIndex(0);
            setInput("");
            return true;
        }
        if (trimmedInput.startsWith("/models")) {
            const modelArg = trimmedInput.split(" ");
            console.log("model arg", modelArg);
            const modelNames = (await availableModels());
            //console.log("available models", modelNames);
            if (modelNames && modelNames.length > 0) {
                const modelName = modelNames[1].model;
                agent.setModel(modelName);
                (0, settings_1.updateSetting)("model", modelName);
                const confirmEntry = {
                    type: "assistant",
                    content: `✓ Switched to model: ${modelArg}`,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, confirmEntry]);
            }
            else {
                const errorEntry = {
                    type: "assistant",
                    content: `Invalid model: ${modelArg}

Available models: ${modelNames.join(", ")}`,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, errorEntry]);
            }
            setInput("");
            return true;
        }
        if (trimmedInput === "/commit-and-push") {
            const userEntry = {
                type: "user",
                content: "/commit-and-push",
                timestamp: new Date(),
            };
            setChatHistory((prev) => [...prev, userEntry]);
            setIsProcessing(true);
            setIsStreaming(true);
            try {
                // First check if there are any changes at all
                const initialStatusResult = await agent.executeBashCommand("git status --porcelain");
                if (!initialStatusResult.success ||
                    !initialStatusResult.output?.trim()) {
                    const noChangesEntry = {
                        type: "assistant",
                        content: "No changes to commit. Working directory is clean.",
                        timestamp: new Date(),
                    };
                    setChatHistory((prev) => [...prev, noChangesEntry]);
                    setIsProcessing(false);
                    setIsStreaming(false);
                    setInput("");
                    return true;
                }
                // Add all changes
                const addResult = await agent.executeBashCommand("git add .");
                if (!addResult.success) {
                    const addErrorEntry = {
                        type: "assistant",
                        content: `Failed to stage changes: ${addResult.error || "Unknown error"}`,
                        timestamp: new Date(),
                    };
                    setChatHistory((prev) => [...prev, addErrorEntry]);
                    setIsProcessing(false);
                    setIsStreaming(false);
                    setInput("");
                    return true;
                }
                // Show that changes were staged
                const addEntry = {
                    type: "tool_result",
                    content: "Changes staged successfully",
                    timestamp: new Date(),
                    toolCall: {
                        id: `git_add_${Date.now()}`,
                        name: "bash",
                        args: { command: "git add ." },
                    },
                    toolResult: addResult,
                };
                setChatHistory((prev) => [...prev, addEntry]);
                // Get staged changes for commit message generation
                const diffResult = await agent.executeBashCommand("git diff --cached");
                // Generate commit message using AI
                const commitPrompt = `Generate a concise, professional git commit message for these changes:

Git Status:
${initialStatusResult.output}

Git Diff (staged changes):
${diffResult.output || "No staged changes shown"}

Follow conventional commit format (feat:, fix:, docs:, etc.) and keep it under 72 characters.
Respond with ONLY the commit message, no additional text.`;
                let commitMessage = "";
                let streamingEntry = null;
                const response = await agent.processUserMessage(commitPrompt);
                if (Array.isArray(response)) {
                    if (!streamingEntry) {
                        const newEntry = {
                            type: "assistant",
                            content: `Generating commit message...\n\n${response[0].content}`,
                            timestamp: new Date(),
                            isStreaming: true,
                        };
                        setChatHistory((prev) => [...prev, newEntry]);
                        streamingEntry = newEntry;
                        commitMessage = response[0].content;
                    }
                    else {
                        commitMessage += response[0].content;
                        setChatHistory((prev) => prev.map((entry, idx) => idx === prev.length - 1 && entry.isStreaming
                            ? {
                                ...entry,
                                content: `Generating commit message...\n\n${commitMessage}`,
                            }
                            : entry));
                    }
                }
                else if (response === "done") {
                    if (streamingEntry) {
                        setChatHistory((prev) => prev.map((entry) => entry.isStreaming
                            ? {
                                ...entry,
                                content: `Generated commit message: "${commitMessage.trim()}"`,
                                isStreaming: false,
                            }
                            : entry));
                    }
                }
                // Execute the commit
                const cleanCommitMessage = commitMessage
                    .trim()
                    .replace(/^["']|["']$/g, "");
                const commitCommand = `git commit -m "${cleanCommitMessage}"`;
                const commitResult = await agent.executeBashCommand(commitCommand);
                const commitEntry = {
                    type: "tool_result",
                    content: commitResult.success
                        ? commitResult.output || "Commit successful"
                        : commitResult.error || "Commit failed",
                    timestamp: new Date(),
                    toolCall: {
                        id: `git_commit_${Date.now()}`,
                        name: "bash",
                        args: { command: commitCommand },
                    },
                    toolResult: commitResult,
                };
                setChatHistory((prev) => [...prev, commitEntry]);
                // If commit was successful, push to remote
                if (commitResult.success) {
                    // First try regular push, if it fails try with upstream setup
                    let pushResult = await agent.executeBashCommand("git push");
                    let pushCommand = "git push";
                    if (!pushResult.success &&
                        pushResult.error?.includes("no upstream branch")) {
                        pushCommand = "git push -u origin HEAD";
                        pushResult = await agent.executeBashCommand(pushCommand);
                    }
                    const pushEntry = {
                        type: "tool_result",
                        content: pushResult.success
                            ? pushResult.output || "Push successful"
                            : pushResult.error || "Push failed",
                        timestamp: new Date(),
                        toolCall: {
                            id: `git_push_${Date.now()}`,
                            name: "bash",
                            args: { command: pushCommand },
                        },
                        toolResult: pushResult,
                    };
                    setChatHistory((prev) => [...prev, pushEntry]);
                }
            }
            catch (error) {
                const errorEntry = {
                    type: "assistant",
                    content: `Error during commit and push: ${error.message}`,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, errorEntry]);
            }
            setIsProcessing(false);
            setIsStreaming(false);
            setInput("");
            return true;
        }
        const directBashCommands = [
            "ls",
            "pwd",
            "cd",
            "cat",
            "mkdir",
            "touch",
            "echo",
            "grep",
            "find",
            "cp",
            "mv",
            "rm",
        ];
        const firstWord = trimmedInput.split(" ")[0];
        if (directBashCommands.includes(firstWord)) {
            const userEntry = {
                type: "user",
                content: trimmedInput,
                timestamp: new Date(),
            };
            setChatHistory((prev) => [...prev, userEntry]);
            try {
                const result = await agent.executeBashCommand(trimmedInput);
                const commandEntry = {
                    type: "tool_result",
                    content: result.success
                        ? result.output || "Command completed"
                        : result.error || "Command failed",
                    timestamp: new Date(),
                    toolCall: {
                        id: `bash_${Date.now()}`,
                        name: "bash",
                        args: { command: trimmedInput },
                    },
                    toolResult: result,
                };
                setChatHistory((prev) => [...prev, commandEntry]);
            }
            catch (error) {
                const errorEntry = {
                    type: "assistant",
                    content: `Error executing command: ${error.message}`,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, errorEntry]);
            }
            setInput("");
            return true;
        }
        return false;
    };
    const processUserMessage = async (userInput) => {
        const userEntry = {
            type: "user",
            content: userInput,
            timestamp: new Date(),
        };
        if (chatHistory.length > 1) {
            chatHistory.splice(0, chatHistory.length - 1);
        }
        setChatHistory((prev) => [...prev, userEntry]);
        setIsProcessing(true);
        setInput("");
        try {
            setIsStreaming(true);
            // Call agent.processUserMessage and expect a single string response
            const response = await agent.processUserMessage(userInput);
            if (typeof response === "string") {
                const assistantEntry = {
                    type: "assistant",
                    content: response,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, assistantEntry]);
            }
            else if (Array.isArray(response)) {
                // If agent still returns an array, join all chunks into a single message
                const fullContent = response.map(chunk => chunk.content).join("");
                const assistantEntry = {
                    type: "assistant",
                    content: fullContent,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, assistantEntry]);
            }
        }
        catch (error) {
            const errorEntry = {
                type: "assistant",
                content: `Error: ${error.message}`,
                timestamp: new Date(),
            };
            setChatHistory((prev) => [...prev, errorEntry]);
            setIsStreaming(false);
        }
        setIsProcessing(false);
        setIsStreaming(false);
        processingStartTime.current = 0;
    };
    (0, ink_1.useInput)(async (inputChar, key) => {
        // Don't handle input if confirmation dialog is active
        if (isConfirmationActive) {
            return;
        }
        if (key.ctrl && inputChar === "c") {
            process.exit(0);
            return;
        }
        // Handle shift+tab to toggle auto-edit mode
        if (key.shift && key.tab) {
            const newAutoEditState = !autoEditEnabled;
            setAutoEditEnabled(newAutoEditState);
            const confirmationService = confirmation_service_1.ConfirmationService.getInstance();
            if (newAutoEditState) {
                // Enable auto-edit: set all operations to be accepted
                confirmationService.setSessionFlag("allOperations", true);
            }
            else {
                // Disable auto-edit: reset session flags
                confirmationService.resetSession();
            }
            return;
        }
        if (key.escape) {
            if (showCommandSuggestions) {
                setShowCommandSuggestions(false);
                setSelectedCommandIndex(0);
                return;
            }
            if (showModelSelection) {
                setShowModelSelection(false);
                setSelectedModelIndex(0);
                return;
            }
            if (isProcessing || isStreaming) {
                agent.abortCurrentOperation();
                setIsProcessing(false);
                setIsStreaming(false);
                setTokenCount(0);
                setProcessingTime(0);
                processingStartTime.current = 0;
                return;
            }
        }
        if (showCommandSuggestions) {
            const filteredSuggestions = (0, command_suggestions_1.filterCommandSuggestions)(commandSuggestions, input);
            if (filteredSuggestions.length === 0) {
                setShowCommandSuggestions(false);
                setSelectedCommandIndex(0);
                // Continue processing the input instead of returning
            }
            else {
                if (key.upArrow) {
                    setSelectedCommandIndex((prev) => prev === 0 ? filteredSuggestions.length - 1 : prev - 1);
                    return;
                }
                if (key.downArrow) {
                    setSelectedCommandIndex((prev) => (prev + 1) % filteredSuggestions.length);
                    return;
                }
                if (key.tab || key.return) {
                    const safeIndex = Math.min(selectedCommandIndex, filteredSuggestions.length - 1);
                    const selectedCommand = filteredSuggestions[safeIndex];
                    setInput(selectedCommand.command + " ");
                    setShowCommandSuggestions(false);
                    setSelectedCommandIndex(0);
                    return;
                }
            }
        }
        if (showModelSelection) {
            if (key.upArrow) {
                const models = await availableModels();
                setSelectedModelIndex((prev) => prev === 0 ? models.length - 1 : prev - 1);
                return;
            }
            if (key.downArrow) {
                const models = await availableModels();
                setSelectedModelIndex((prev) => (prev + 1) % models.length);
                return;
            }
            if (key.tab || key.return) {
                const availableModel = await availableModels();
                const selectedModel = availableModel[selectedModelIndex];
                agent.setModel(selectedModel.model);
                (0, settings_1.updateSetting)("model", selectedModel.model);
                const confirmEntry = {
                    type: "assistant",
                    content: `✓ Switched to model: ${selectedModel.model}`,
                    timestamp: new Date(),
                };
                setChatHistory((prev) => [...prev, confirmEntry]);
                setShowModelSelection(false);
                setSelectedModelIndex(0);
                return;
            }
        }
        if (key.return) {
            const userInput = input.trim();
            if (userInput === "exit" || userInput === "quit") {
                process.exit(0);
                return;
            }
            if (userInput) {
                const directCommandResult = await handleDirectCommand(userInput);
                if (!directCommandResult) {
                    await processUserMessage(userInput);
                }
            }
            return;
        }
        if (key.backspace || key.delete) {
            const newInput = input.slice(0, -1);
            setInput(newInput);
            if (!newInput.startsWith("/")) {
                setShowCommandSuggestions(false);
                setSelectedCommandIndex(0);
            }
            return;
        }
        if (inputChar && !key.ctrl && !key.meta) {
            const newInput = input + inputChar;
            setInput(newInput);
            if (newInput.startsWith("/")) {
                setShowCommandSuggestions(true);
                setSelectedCommandIndex(0);
            }
            else {
                setShowCommandSuggestions(false);
                setSelectedCommandIndex(0);
            }
        }
    });
    return {
        input,
        showCommandSuggestions,
        selectedCommandIndex,
        showModelSelection,
        selectedModelIndex,
        commandSuggestions,
        availableModels,
        agent,
        autoEditEnabled,
    };
}
//# sourceMappingURL=use-input-handler.js.map