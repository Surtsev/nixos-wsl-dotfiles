"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const client_1 = require("./client"); // adjust path
const tools_1 = require("./tools"); // adjust path
async function main() {
    const client = new client_1.ollamacodeClient("qwen3:8b");
    console.log("Current model:", client.getCurrentModel());
    // console.log("=== Testing normal chat ===");
    const response = await client.chat([
        { role: "user", content: "crete a file name new.py" },
    ], tools_1.ollamacode_TOOLS, { search_parameters: { mode: "auto" } });
    console.log("Response:", response.choices[0].message?.toolCalls || "no content");
    // console.log("\n=== Testing streaming chat ===");
    // for await (const c of client.chatStream([
    //     { role: "user", content: "create final.txt file nd write a todo app code in reactjs and paste it in the file" },
    // ],
    //     ollamacode_TOOLS,
    //     { search_parameters: { mode: "auto" } })) {
    //     if (c.type === "message"){
    //         process.stdout.write(c.choices[0].message.content);
    //     }
    //     if (c.type === "tool_calls" && c.toolCalls.length > 0 ){
    //         console.log("\n🔧 Tool call:",c.toolCalls);
    //     }
    //     if (c?.tool_result) {
    //         console.log("\n📥 Tool result:");
    //     }
    //     if (c.type === "done") {
    //         console.log("\n✅ Stream finished.");
    //     }
    // }
    console.log("\nDone.");
}
main().catch(console.error);
//# sourceMappingURL=test.js.map